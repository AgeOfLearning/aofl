const projectFileNames = {
  GENERATED_CONFIG: ['.aofl.json'],
  CONFIG: ['.aofl.js'],
};

const environments = {
  DEVELOPMENT: 'development',
  PRODUCTION: 'production',
  TEST: 'test'
};

module.exports = {
  projectFileNames,
  environments
};
