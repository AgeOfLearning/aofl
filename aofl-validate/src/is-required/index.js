/**
 * isValidFormat
 *
 * @param {String} value
 * @return {Boolean} True or false if the given value is an
 *                   empty string.
 */
function isRequired(value) {
  return value ? true : false;
}

export {
  isRequired
};
